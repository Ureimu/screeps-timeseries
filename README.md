# screeps-timeseries

the package is used for storing time series data (integer only) in screeps, a coding game.

use [utf15](https://github.com/screepers/utf15) as data handler.

## install
```
npm install screeps-timeseries
```
